FROM arm64v8/alpine:latest

EXPOSE 1194/udp
EXPOSE 1194/tcp

ADD entrypoint.sh /etc/openvpn/entrypoint.sh
ADD pem.py /etc/openvpn/pem.py

RUN apk add openvpn bash easy-rsa python3 && \
    mkdir /etc/openvpn/data

CMD ["bash", "/etc/openvpn/entrypoint.sh"]
